###老男孩python学员###
qq群昵称：71-sungwd-顾伟栋
svn用户名:stu107411
博客：http://www.cnblogs.com/sunface/
github:https://github.com/sunpython/python
qq:77625811
邮箱:77625811@qq.com
#######################
    flow-process 流程图文件夹
    user_login.py 用户登陆作业
    tree_list.py 三层菜单作业
    readme 第1天的文件说明
运行要示：python3.x环境中运行，请确保您的系统支持unique

user_login.gy功能及细节

    数据：

    1.把账号存放在user.txt中。用户和密码用"|"分隔，每一行一个账号
    2,error.log中存放被锁定的用户
    细节：

    1，用户输入用户名
    2，判断是否存在此用户，如果不存在，则再次提示输入用户，否则判断用户有没有存放在锁定文件中
    3，如果有，虽再提示输入用户名，否则提示输入密码
    4，从user.txt中取出数据与刚输入的用户和密码比对，如果正确，则打印验证成功消息，程序结束运行;否则记录错误次数
    5，若错误次数未到3次，则再次要求输入密码，否则程序运行结束！

三层菜单：
    数据格式：定义字典并嵌套列表
    流程：
    1,定义字典
    2，循环字典
    3，取出数据并要求输入b,q
    4,如果输入的是数字，则再到内循环查找数据
    5，如果是字母，则让b退出本次循环，q退出整个循环

